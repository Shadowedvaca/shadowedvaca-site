# Copy this file to secrets.yaml and fill in real values.
# secrets.yaml is git-ignored and must NEVER be committed.
# Deploy to the server manually: scp monitoring/config/secrets.yaml deploy@5.78.114.224:/opt/sv-monitoring/config/

smtp:
  host: "smtp.gmail.com"
  port: 587
  username: "alerts@example.com"
  # Use a Gmail App Password (not your account password)
  # Generate at: https://myaccount.google.com/apppasswords
  password: "your-app-password-here"

# Optional: direct API usage endpoint access (not required for Phase 1)
anthropic_admin_api_key: ""
openai_api_key: ""
